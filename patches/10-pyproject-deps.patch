From: Principal Engineer <review@prod.harden>
Subject: [PATCH 10/10] Add missing production dependencies

The fixes require asyncpg for health checks and proper logging.
Add them as explicit dependencies.

--- pyproject.toml
+++ pyproject.toml
@@ -1,10 +1,19 @@
 [project]
 name = "multi-agent-supervisor"
 version = "0.2.0"
 description = "Multi-Agent AI Software Development Team"
 dependencies = [
     "langgraph>=0.2.0",
     "langchain-openai>=0.1.0",
     "fastapi>=0.110.0",
     "pydantic>=2.0",
     "httpx>=0.27.0",
     "mcp>=1.0.0",
+    # ── Production dependencies ──
+    "asyncpg>=0.29.0",
+    "uvicorn[standard]>=0.29",
+    "python-json-logger>=2.0",
+    "prometheus-client>=0.20",
 ]
 
 [project.optional-dependencies]
 dev = [
     "pytest>=8.0",
     "pytest-asyncio>=0.23",
     "pytest-cov>=5.0",
+    "respx>=0.21",
+    "asgi-lifespan>=2.1",
 ]
